# 2026-09-04 · 首轮实机验证

## 环境
- venv：`C:/Users/Chinese/.workbuddy/binaries/python/envs/default`
- 依赖：requests + pycryptodome
- 脚本：`scripts/verify_endpoint.py`（自研，全版本 Key0 派生）

## 结果
3 个核心加密端点全部解密成功：
1. `/api/futures/home/statistics` → v=77 → OI/爆仓/多空/资金总览
2. `/api/spot/rsi/list` → v=66 → 407 币 RSI 排行
3. `/api/openInterest/v3/chart` + `/api/futures/liquidation/chart` → v=55 → OI 序列、逐所爆仓

## 关键发现
- **当前线上 v 值 = 55/66/77（遗留常量表）**，与社区文档（v=1 通用）不符 → 密钥版本确实在轮换
- 无 key / cookie / session，响应自带 `user` + `v` 头即可解密 ✅
- 资金费率端点路径未知（2 个候选均 404）→ 留 Phase 2 用浏览器 Network 抓

## 下一步
- Phase 1：把 `scripts/verify_endpoint.py` 提炼为 `src/coinglass` 包（client/crypto/endpoints）
- Phase 2：浏览器抓包补全端点目录 + 资金费率真实路径
